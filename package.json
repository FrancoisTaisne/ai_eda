{
  "name": "ai_eda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm --prefix aieda_js run build",
    "check": "npm --prefix aieda_js run check"
  },
  "private": true
}
